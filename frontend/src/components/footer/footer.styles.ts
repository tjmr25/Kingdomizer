import { css } from "lit";

export const footerStyles = css`
    footer {
      background-color: var(--color-main-bluegrey); /* Hintergrundfarbe */
      color: white; /* Schriftfarbe */
      padding: 0.5rem;
      text-align: center;
      font-size: 0.875rem;
    }
`;
